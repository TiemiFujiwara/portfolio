{
  "name": "tiemi-fujiwara-portfolio",
  "version": "1.0.0",
  "description": "Portfolio pessoal de Tiemi Fujiwara",
  "private": true,
  "scripts": {
    "serve": "firebase serve --only hosting",
    "emulate": "firebase emulators:start --only hosting",
    "deploy": "firebase deploy --only hosting",
    "deploy:preview": "firebase hosting:channel:deploy preview --expires 7d"
  },
  "devDependencies": {
    "firebase-tools": "^13.30.0"
  }
}
